<h1><%= piece.title %></h1>
<div class="row">
    <div class="col">
        <img src="<%= piece.primaryImage %>" width="100%">
    </div>
    <div class="col">
        <b>Artist:</b><% if (piece.constituents == null) {%>
                          <i> No artist information</i> 
                      <%} else {%>
                          <a href="<%= piece.artistWikidata_URL %>"><%= piece.artistDisplayName %></a>. 
                          <%= piece.artistDisplayBio %>
                      <% } %><br>
        <br>
        <b>Description:</b>
            <%= piece.title %> is dated <%= piece.objectDate %>, during the <%= piece.period %> period.
            The <%= piece.objectName %> is made with <%= piece.medium %>
            <% if (piece.culture != "") { %>
                , and is of <%= piece.culture %> culture
            <% } %>. 
            Obtained in <%= piece.accessionYear %>, this piece currently resides at the <%= piece.repository %>.
            <% if (piece.isHighlight) { %>
                This is a highlighted work in the collection.
            <%}%>
            

    </div>
    <div class="col">
        <b>Department:</b> <%= piece.department %> <br>
        <b>Classification:</b> <%= piece.classification %> <br>
        <b>Source:</b> <%= piece.creditLine %> <br><br>
        <b>Dimensions:</b> <%= piece.dimensions %> <br><br>
        
        <b>Gallery Number:</b> <% if (piece.GalleryNumber == "") {%>
                                    <i> Not Available</i> 
                                <%} else {%>
                                    <%= piece.GalleryNumber %> 
                                <%}%><br><br>
        <a href="<%= piece.objectURL %>">Link to art piece on MET site</a> <br>
        <br>
        <% if (starred) { %>
            <a href="/starred/remove/<%= piece.objectID %>" %><button class="btn btn-medium btn-info">Remove from Starred</button></a>
        <% } else { %>
            <a href="/addObject/<%= piece.objectID %>" %><button class="btn btn-medium btn-info">Add to Starred</button></a>
        <% } %>
    </div>
</div>

